<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nexon.cyphers.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".story.supermoon.SuperMoonActivity"></activity>
        <activity android:name=".story.bluemoon.BlueMoonActivity" />
        <activity android:name=".story.eclipse.EclipseActivity" />
        <activity android:name=".story.StoryActivity" />
        <activity android:name=".PositionActivity" />
        <activity android:name=".Toosin.ToosinActivity" />
        <activity android:name=".MatchingDeatilActivity" />
        <activity android:name=".WebContentActivity" />
        <activity android:name=".LoadingActivity" />
        <activity android:name=".Matching_result" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>